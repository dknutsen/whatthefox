<div class="max-w-sm rounded overflow-hidden shadow-lg m-2">
  <img class="w-full" src="images/{{imgName}}" alt="{{this.title}}">
  <div class="px-6 py-4">
    <div class="font-bold text-xl mb-2">{{this.title}}</div>
    <p class="text-grey-darker text-base">
      {{this.caption}}
    </p>
  </div>
</div>
