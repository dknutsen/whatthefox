<nav role="navigation" class="flex items-center justify-between flex-wrap bg-red p-6">

  {{!-- brand --}}
  {{#link-to 'index'}}
  <div class="flex items-center flex-no-shrink text-white mr-6">
    <img class="h-8" src="images/zooshelf-white.png" >
  </div>
  {{/link-to}}

  {{!-- hamburger --}}
  <div class="block lg:hidden">
    <button {{action 'toggleNav'}} class="flex items-center px-3 py-2 border rounded text-red-lighter border-red-light hover:text-white hover:border-white">
      <svg class="fill-current h-3 w-3" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><title>Menu</title><path d="M0 3h20v2H0V3zm0 6h20v2H0V9zm0 6h20v2H0v-2z"/></svg>
    </button>
  </div>

  {{!-- nav list --}}
  <div class="w-full block flex-grow lg:flex lg:items-center lg:w-auto {{if toggleNav 'block' 'hidden'}}">
    <div class="text-lg lg:flex-grow color-white">

      {{!-- link-to's here with same class names as this example --}}
      {{#link-to "foxes" class="block mt-4 lg:inline-block lg:mt-0 text-red-lighter hover:text-white mr-4"}}
        Foxes
      {{/link-to}}
      {{#link-to "blackbears" class="block mt-4 lg:inline-block lg:mt-0 text-red-lighter hover:text-white mr-4"}}
        Black Bears
      {{/link-to}}
      {{#link-to "lemmings" class="block mt-4 lg:inline-block lg:mt-0 text-red-lighter hover:text-white mr-4"}}
        Lemmings
      {{/link-to}}
      {{#link-to "sealions" class="block mt-4 lg:inline-block lg:mt-0 text-red-lighter hover:text-white mr-4"}}
        Sea Lions
      {{/link-to}}
      {{#link-to "rams" class="block mt-4 lg:inline-block lg:mt-0 text-red-lighter hover:text-white mr-4"}}
        Sheep
      {{/link-to}}

    </div>
  </div>
</nav>

